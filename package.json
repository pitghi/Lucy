{
  "name": "lucy",
  "private": true,
  "version": "0.1.0",
  "description": "Lucy — evaluation et recommandation de produits cosmetiques basees sur la concentration estimee des ingredients",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
